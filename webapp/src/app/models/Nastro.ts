export class Nastro {
    id: number
    name: string
    sezione_id: number

    constructor(id: number, name: string, sezione_id: number) {
        this.id = id
        this.name = name
        this.sezione_id = sezione_id
    }
}